<table class="table table-striped table-hover">
  <thead>
    <tr>
      <th>representation</th>
      <th>title</th>
      <th>created by</th>
      <th>last update by</th>
      <th>controls</th>
    </tr>
  </thead>
  <tbody>
      <tr>
        <td><%= cl_image_tag(@document.cloudinary_uri, :crop => :fill, 
          :width => 120, :height => 80) %></td>
        <td>
          <%= link_to @document.title, 
            link_to_cloudinary(@document.cloudinary_uri), 
            target: "_blank" %>
        </td>
        <td>
          <% unless @document.creator <= 0 %>
            <%= link_to @project.users.find_by_id(@document.creator).name, 
              @project.users.find_by_id(@document.creator) %>
          <% else %>
            Unknown
          <% end %>
           at <%= @document.created_at %>
        </td>
        <td>
          <% unless @document.last_editor.nil? %>
            <%= link_to @users.find_by_id(@document.last_editor).name, 
              @users.find_by_id(@document.last_editor) %> 
            at <%= @document.updated_at %>
          <% else %>
            -
          <% end %>
        </td>
        <td>
          <ul class="documents-controls">
            <li><%= link_to 'Edit', edit_document_url %></li>
            <li><%= link_to 'Delete', @document, method: :delete, data: { confirm: 'Are you sure?' } %></li>
          </ul>
        </td>
      </tr>
  </tbody>
</table>